"use strict";
let myAge = 30;
let myName = "Allan";
let isEmployee = true;
let workingNow;
function addNumbers(firstNumber, secondNumber, showResult, phrase) {
    const result = number1 + number2;
    if (showResult) {
        console.log(phrase + result);
    }
    else {
        return result;
    }
}
const number1 = 20;
const number2 = 500;
const printRedult = true;
const resultPhrase = "Result is: ";
addNumbers(number1, number2, printRedult, resultPhrase);
//# sourceMappingURL=build-in-types.js.map